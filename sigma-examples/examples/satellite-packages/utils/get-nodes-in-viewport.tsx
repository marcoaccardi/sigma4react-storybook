/**
 * Utils - Get Nodes in Viewport
 *
 * TODO: Convert from Storybook example
 *
 * This example will demonstrate:
 * - Using getNodesInViewport utility from @sigma/utils
 * - Querying visible nodes in current viewport
 * - Viewport-based node filtering
 */

import type { FC } from "react";

export const GetNodesInViewport: FC = () => {
  return (
    <div style={{ height: "100%", width: "100%", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <p>TODO: Implement Utils Get Nodes in Viewport example</p>
    </div>
  );
};
